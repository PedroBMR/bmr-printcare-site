<section class="section-shell split-section" aria-labelledby="local-first-title">
  <div>
    <p class="section-eyebrow">Local-first approach</p>
    <h2 id="local-first-title">Built for workshops that want control over their records.</h2>
  </div>
  <div class="section-card local-card">
    <p>
      The product direction is local-first: maintenance records should be useful on the desktop, easy to understand,
      and not dependent on a hosted account just to manage printer care.
    </p>
    <div class="local-note">
      <strong>Website analytics note</strong>
      <span>The website may use conditional GA4 for basic measurement when configured. App maintenance records are planned to remain local-first and are not sent to GA4.</span>
    </div>
  </div>
</section>

<style>
  .local-card p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 1.05rem;
  }

  .local-note {
    display: grid;
    gap: 0.35rem;
    margin-top: 1.5rem;
    border: 1px solid rgba(57, 208, 255, 0.24);
    border-radius: 1.1rem;
    padding: 1rem;
    background: rgba(57, 208, 255, 0.07);
  }

  .local-note span {
    color: var(--color-text-muted);
  }
</style>
