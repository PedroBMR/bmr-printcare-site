<section class="section-shell split-section" aria-labelledby="problem-title">
  <div>
    <p class="section-eyebrow">The problem</p>
    <h2 id="problem-title">Printer maintenance becomes scattered fast.</h2>
  </div>
  <div class="section-card problem-card">
    <p>
      Notes live in spreadsheets, chat messages, slicer profiles, paper labels, and memory. As a printer fleet grows,
      it gets harder to know what was serviced, which parts were changed, and what needs attention next.
    </p>
    <ul>
      <li>Recurring maintenance tasks are easy to miss.</li>
      <li>Part changes and service notes are often disconnected.</li>
      <li>Cloud tools can feel unnecessary for local workshop records.</li>
    </ul>
  </div>
</section>

<style>
  .problem-card p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 1.05rem;
  }

  ul {
    display: grid;
    gap: 0.85rem;
    margin: 1.5rem 0 0;
    padding: 0;
    list-style: none;
  }

  li {
    display: flex;
    gap: 0.75rem;
    color: var(--color-text);
  }

  li::before {
    width: 0.5rem;
    height: 0.5rem;
    flex: 0 0 auto;
    border-radius: 999px;
    margin-top: 0.65rem;
    background: var(--color-accent);
    content: '';
    box-shadow: 0 0 18px rgba(57, 208, 255, 0.55);
  }
</style>
