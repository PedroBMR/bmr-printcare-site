<section id="roadmap" class="section-shell roadmap-section" aria-labelledby="roadmap-title">
  <div>
    <p class="section-eyebrow">Roadmap / Coming soon</p>
    <h2 id="roadmap-title">The public release path is still being prepared.</h2>
    <p>
      Download links are not available yet. The next milestones will expand the website content, clarify product scope,
      and prepare a responsible path for roadmap updates and future availability.
    </p>
  </div>
  <span class="cta-pill">Roadmap updates coming soon</span>
</section>

<style>
  .roadmap-section {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 1.5rem;
    align-items: center;
    border: 1px solid rgba(57, 208, 255, 0.26);
    border-radius: calc(var(--radius-card) + 0.35rem);
    padding: clamp(1.5rem, 5vw, 3rem);
    background:
      radial-gradient(circle at 90% 20%, rgba(57, 208, 255, 0.24), transparent 18rem),
      linear-gradient(135deg, rgba(16, 22, 36, 0.9), rgba(21, 29, 46, 0.72));
  }

  p:not(.section-eyebrow) {
    max-width: 48rem;
    margin: 1rem 0 0;
    color: var(--color-text-muted);
    font-size: 1.05rem;
  }

  .cta-pill {
    display: inline-flex;
    min-height: 3rem;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 0.85rem 1.15rem;
    background: var(--color-text);
    color: var(--color-background);
    font-weight: 900;
    text-decoration: none;
    white-space: nowrap;
  }

  @media (max-width: 720px) {
    .roadmap-section {
      grid-template-columns: 1fr;
    }

    .cta-pill {
      width: 100%;
    }
  }
</style>
