<section class="section-shell scope-section" aria-labelledby="scope-title">
  <div class="section-heading">
    <p class="section-eyebrow">Product scope</p>
    <h2 id="scope-title">What NozzleNote is / is not.</h2>
    <p>
      NozzleNote by BMR is being shaped for makers, advanced hobbyists, personal and growing 3D printing setups,
      and small print businesses that want maintenance history and organization.
    </p>
  </div>

  <div class="scope-grid">
    <article class="section-card">
      <h3>What it is</h3>
      <p>
        NozzleNote is planned as a local-first maintenance logbook for 3D printers — designed to track routines, parts, service notes, printer history and workshop observations. Calibration and print-quality records remain roadmap-only.
      </p>
    </article>
    <article class="section-card">
      <h3>What it is not</h3>
      <p>
        NozzleNote is not a slicer, remote printer controller, automated failure detector, cloud print farm manager, manufacturer service portal, or manufacturer validated tool.
      </p>
    </article>
  </div>

  <p class="strategic-line">
    It does not try to control your printers. It helps you understand how they have been maintained.
  </p>
</section>

<style>
  .scope-section {
    display: grid;
    gap: 1.5rem;
  }

  .scope-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  h3 {
    margin: 0;
    font-size: 1.25rem;
  }

  p {
    margin: 0.75rem 0 0;
    color: var(--color-text-muted);
  }

  .strategic-line {
    border-left: 3px solid var(--color-accent);
    margin: 0;
    padding: 0.25rem 0 0.25rem 1rem;
    color: var(--color-text);
    font-size: 1.1rem;
    font-weight: 800;
  }

  @media (max-width: 760px) {
    .scope-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
