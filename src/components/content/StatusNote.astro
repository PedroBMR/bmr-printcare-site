---
interface Props {
  title: string;
  tone?: 'default' | 'accent';
}

const { title, tone = 'default' } = Astro.props;
---

<aside class:list={["status-note", `status-note--${tone}`]}>
  <h2>{title}</h2>
  <slot />
</aside>
